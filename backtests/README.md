# Backtests

Backtesting results and performance reports.

Each strategy should have its own folder with:
- `config.yaml` — Strategy parameters
- `results/` — Performance metrics and charts
- `logs/` — Backtest execution logs
